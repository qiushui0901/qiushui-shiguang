spring:
  application:
    name: shiguang-recommend # 应用名称
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        group: DEFAULT_GROUP
        namespace: shiguang
        server-addr: 127.0.0.1:8848
